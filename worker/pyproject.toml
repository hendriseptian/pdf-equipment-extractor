[project]
name = "piping-tag-extractor"
version = "1.0.1"
description = "P&ID piping tag extractor using Gemini vision"
requires-python = ">=3.13"
dependencies = [
  "fastapi>=0.115,<1",
  "pydantic>=2,<3",
]

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
